name: include_headings
description: >-
  Whether or not headings should be included in the Bible content.
  The default for this field is false unless the reference is a
  chapter or introduction (GEN.1 or GEN.INTRO1) in which case it would be true.
in: query
required: false
schema:
  type: boolean
